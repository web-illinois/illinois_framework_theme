{#
/**
 * @file
 * Default theme implementation to display a paragraph.
 *
 * Available variables:
 * - paragraph: Full paragraph entity.
 *   Only method names starting with "get", "has", or "is" and a few common
 *   methods such as "id", "label", and "bundle" are available. For example:
 *   - paragraph.getCreatedTime() will return the paragraph creation timestamp.
 *   - paragraph.id(): The paragraph ID.
 *   - paragraph.bundle(): The type of the paragraph, for example, "image" or "text".
 *   - paragraph.getOwnerId(): The user ID of the paragraph author.
 *   See Drupal\paragraphs\Entity\Paragraph for a full list of public properties
 *   and methods for the paragraph object.
 * - content: All paragraph items. Use {{ content }} to print them all,
 *   or print a subset such as {{ content.field_example }}. Use
 *   {{ content|without('field_example') }} to temporarily suppress the printing
 *   of a given child element.
 * - attributes: HTML attributes for the containing element.
 *   The attributes.class element may contain one or more of the following
 *   classes:
 *   - paragraphs: The current template type (also known as a "theming hook").
 *   - paragraphs--type-[type]: The current paragraphs type. For example, if the paragraph is an
 *     "Image" it would result in "paragraphs--type--image". Note that the machine
 *     name will often be in a short form of the human readable label.
 *   - paragraphs--view-mode--[view_mode]: The View Mode of the paragraph; for example, a
 *     preview would result in: "paragraphs--view-mode--preview", and
 *     default: "paragraphs--view-mode--default".
 * - view_mode: View mode; for example, "preview" or "full".
 * - logged_in: Flag for authenticated user status. Will be true when the
 *   current user is a logged-in member.
 * - is_admin: Flag for admin user status. Will be true when the current user
 *   is an administrator.
 *
 * @see template_preprocess_paragraph()
 *
 * @ingroup themeable
 */
#}

{% set background_color = content.field_contact_background['#items'].0.value %}
{% set image_float = content.field_contact_layouts %}
{%
  set classes = [
  'paragraph',
  'paragraph--type--' ~ paragraph.bundle|clean_class,
  view_mode ? 'paragraph--view-mode--' ~ view_mode|clean_class,
  not paragraph.isPublished() ? 'paragraph--unpublished',
  'il-formatting col-lg-12 col-md-12 col-sm-12 d-flex row full-width ' ~ 'background--color--' ~ background_color
]
%}

{% block paragraph %}
  {# Paragraph ID used for anchor linking #}
  <div {{ attributes.setAttribute('id', 'paragraph--' ~ paragraph.id() ) }} {{ attributes.addClass(classes) }}>
    {% block content %}
{#      {{ kint(content) }}#}
    <div class="d-flex row col-lg-12 col-md-12 fixed-width">
      <div class="d-flex col-lg-12">
        <h2>{{ content.field_contact_title }}</h2>
      </div>
      <div class="d-flex col-lg-6 col-md-6">
        <div class="field--name-field-contact__content__wrapper">
          <div class="field--name-field-contact__title">
            <p>{{ content.field_contact_content.0['#paragraph'].field_contact_content_title.value }}</p>
          </div>
          <div class="field--name-field-contact__subtitle">
            <p>{{ content.field_contact_content.0['#paragraph'].field_contact_content_subtit.value }}</p>
          </div>
          <div class="field--name-field-contact__address">
            <address>{{ content.field_contact_content.0['#paragraph'].field_contact_content_address.value|raw }}</address>
          </div>
          <div class="field--name-field-contact__phone">
            <p>{{ content.field_contact_content.0['#paragraph'].field_contact_content_phone.value }}</p>
          </div>
          <div class="field--name-field-contact__email">
            <p>{{ content.field_contact_content.0['#paragraph'].field_contact_content_email.value }}</p>
          </div>
          <div class="field--name-field-contact__button">
            <a class="il-button" href="#">{{ content.field_contact_content.0['#paragraph'].field_contact_content_button.value }}</a>
          </div>
        </div>
        <div class="d-flex col-lg-6 col-md-6">
          <div class="field--name-field-contact__image__wrapper">
{#            <img src="{{ base_path_http ~ file_url() }}"/>#}

          </div>
        </div>

      </div>
      <div class="col-lg-12">
        {{ content.field_contact_body }}
      </div>
    </div>
    {% endblock %}
  </div>
{% endblock paragraph %}
