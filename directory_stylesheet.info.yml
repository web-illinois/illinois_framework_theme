name: 'Directory Stylesheet'
description: 'CSS for directory pages'
package: directory_stylesheet

type: theme
core: 8.x

# Defines libraries group in which we can add css/js.
libraries:
  - directory_stylesheet/directory_stylesheet
